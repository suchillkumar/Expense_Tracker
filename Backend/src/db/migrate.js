// MongoDB does not require manual migrations.
// Mongoose schemas handle collection creation automatically.
// Run `node src/db/seed.js` to seed demo data.
console.log('MongoDB: Migrations are handled by Mongoose schemas. No action needed.')
console.log('Run `npm run seed` to seed demo data.')
